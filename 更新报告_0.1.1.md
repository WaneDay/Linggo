# Linggo 更新报告 / 更新记录

**版本**：0.1.0 → **0.1.1**
**日期**：2026-09-19
**产物（本次更新包）**
- 安装包：`Linggo_0.1.1_x64-setup.exe`（10.2 MB，NSIS）
- 独立可执行：`target\release\Linggo.exe`（62.6 MB）
- MSI：`Linggo_0.1.1_x64_en-US.msi`

> 升级方式：直接双击安装，检测到旧版本（0.1.0）自动覆盖升级，设置与词库数据（`%APPDATA%\com.linggo.app`）保留。

---

## 一、本次修复总览

本次共修复 **6 类症状**，根结是：

> 此前多次「改了但没用」的真相 —— 项目的 `llama_backend.rs` 里存在**重复定义的 `is_bad_output` / `norm_eq`**（函数被定义两遍/三遍）+ 一处**残余的多余 `}`**，导致 `cargo` 编译一直失败、**可执行文件从未真正更新过**。本次已清除全部重复定义并收敛唯一版本，编译（`cargo build --release`）通过。

顺带补齐两件一起修好的底层问题：

- **前端 `dist` 从未嵌入 exe**：本项目只执行了 `cargo build`（不会构建前端），导致 release 版 WebView 加载不到 `index.html` 而显示「无法访问此页面」。**正确链为 `npx tauri build`**（先 `npm run build` 生成前端、再编译 Rust、再打 NSIS），本次即用此链产出，前端已内嵌。
- **`is_bad_output` 调用实参修正**：三处调用点用了错误的变量名（如 `&clean` / `&got_eq_original_candidate`，均不存在），本次已统一为正确的实参。

---

## 二、六症状 —— 触发句、修复前、修复后

| # | 触发示例 | 修复前（错误输出） | 修复后（正确输出） |
|---|----------|--------------------|--------------------|
| 1 | `#insane# hey` | `#insane#`（`#…#` 包裹被原样回声） | `嗨`（剥掉 `#…#`，只剩译文） |
| 2 | `crazy` | `crazy\nChinese:`（英文原文 + 尾部 `Chinese:` 语名标签） | 纯中译（剥尾部语名标签行） |
| 3 | `疯` | `疯\n中文:`（⭐重复：`中文:` 尾语名标签） | 纯中译，无尾标签 |
| 4 | `疯狂` | `「疯狂」\nChinese:`（引号包裹 + 尾 `Chinese:`） | 纯中译，无引号包裹、无尾标签 |
| 5 | `教えてください` | `中国語: 教えてください`（CJK 目标语名前缀 `中国語:` 混入译文） | `请教教我`（剥 `{目标语名}: ` 前缀） |
| 6 | `nice` 等 | `the user says "..."` 引导语 / `感兴趣` 前导语感染译文 | 只留译文（剥引导语/前导型剥除） |

---

## 三、修复清单（文件级明细）

### 1. `src\llama_backend.rs`
- **清除重复定义**：`is_bad_output` 原定义两遍（424 与 442 行）、`norm_eq` 原定义三遍（431/451/463 行）→ 统一收敛为**唯一一份**（双参版 `is_bad_output(translated, original)` + `norm_eq`）。
  - 双参版与新签名（`is_bad_output(&got, &seg)`）以及四处新旧调用点全部匹配，不再有重复符号、编译失败。
- **586-589 区域**：删除因重复定义残留的多余 `}`（曾致 `cargo` 报 `unexpected closing delimiter`）。
- **实参修正**：三处错误实参（非作用域内的 `clean`、`got_eq_original_candidate`）改为正确的 `seg`，`is_bad_output` 判定能真正针对当前段落生效。

### 2. `src\prompt.rs`
- **剥 `#…#` 短词回声**：模型把 `#insane#` 等 `#词#` 包裹原样吐出时，剥掉两端 `#`，还原为译文载体（`#…#` 包裹 = 短词回声信号）。
- **剥 CJK 目标语名前缀**：`中国語: 教えてください` / `日本語: …` / `한국어: …` 等 → 剥掉 `{目标语名}: ` 前缀，杜绝 `中国語:` 混进译文。
- **剥尾部语名标签行**：`疯狂\nChinese:` / `疯\n中文:` / `教えてください\n中国語:` 等 → 剥掉尾部 `\n{语名}:` 回声行。
  - 支持语名不限于 ASCII，CJK 目标名（`中国語`/`中文`/`日本語`/`한국어` 等）也能识别剥除。
- **剥前端引导语**：`the user says "..."` / `Translated: ...` / `译文：` 等前导空壳文本统一剥除，保留纯译文。
- **`#…#` 剥链增强**：若剥完 `#…#` 后只剩原文（对原文的重复），会走「配对补全」格式重试一次，确保真正得到译文而非回声。

---

## 四、回归验证方式（安装最新版后逐一测试）

1. 输入 `#insane# hey` → 期望输出：`嗨`，**不含** `#…#`
2. 输入 `crazy` → 期望输出：纯中文译文，**不含** `\nChinese:`
3. 输入 `疯` → 期望输出：纯中译，**不含** `\n中文:`
4. 输入 `疯狂` → 期望输出：纯中译，**不含** `「…」` 包裹、**不含** `\nChinese:`
5. 输入 `教えてください` → 期望输出：`请教教我`，**不含** `中国語:` 前缀
6. 输入英文短句（如 `nice`）→ 期望输出：纯中译，**不含** `the user says` 引导语

---

## 五、技术栈与构建

- 平台：Windows x64（Tauri v2 + WebView2）
- 前端：Vite + TypeScript（`npm run build` → `dist/`）
- 后端：Rust（`llama_backend.rs` / `prompt.rs` / `mt_engine.rs`）
- 打包：`npx tauri build`（嵌入前端 → 编译 Rust → NSIS/MSI）

**关键教训（此文件永久留存供后续参考）**
- **改前端/入口文件后必须走 `npx tauri build`**，单跑 `cargo build` 不会刷新 WebView 的前端资源。
- 修复「编译不过/不生效」时，先 `cargo build` 确认零报错，再打安装包；不要对同一文件的多副本各改一半。
- 项目源码唯一权威目录：`D:\Project folder\extends\linggooo\MT2Flash\`（同名副本目录一律忽略，避免改错）。
